package middleware

import (
	// "zen_computer/helper"

	"github.com/gin-gonic/gin"
)


func AuthMiddleware(ctx *gin.Context) {
	// user := helper.ParseHeaderAuthorization(ctx)

    // if !user {
		
    // }

	// ctx.Set("userId", user.ID)
	// ctx.Set("role", user.Role)

	// ctx.Next()
}
